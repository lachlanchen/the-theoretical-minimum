# Visual Evidence
## Frame Inventory
- `lecture_01_figure_02.png`: A single upper-left hump-like sketch appears on an otherwise mostly empty board, consistent with one localized Schrödinger wave function; this screenshot should remain in the final notes as visual evidence and be paired with a clean schematic redraw.
- `lecture_01_figure_03.png`: Two separated hump-like sketches are visible across the upper board, with faint point markers beneath them; this screenshot should remain in the final notes and be shown alongside a cleaned two-center TikZ reconstruction.
- `lecture_01_figure_04.png`: The board shows the same two upper wave-function sketches plus a lower qualitative graph with axes and a rising curve; this screenshot should remain in the final notes and be paired with a redraw of the two-tier board layout.
- `lecture_01_figure_05.png`: A cropped particle table shows the headers `Name`, `Symbol`, and `type`, plus the photon row with `Photon`, a `\gamma`-like mark, and `A`; this screenshot should remain in the final notes and be paired with a cleaned table fragment.

## Equation Extraction
- [partially visible] `P` or a similar single-letter point label beneath the marked point in `lecture_01_figure_02.png`
- [partially visible] `P` or a similar single-letter point label beneath each marked point in `lecture_01_figure_03.png`
- [standard completion] `\psi(x)` for the single localized hump in `lecture_01_figure_02.png`
- [standard completion] `\psi_1(x),\ \psi_2(x)` for the two separated localized profiles in `lecture_01_figure_03.png`
- [standard completion] `\dfrac{\psi_1(x)+\psi_2(x)}{\sqrt{2}}` as the approximate equilibrium superposition discussed in the transcript near `lecture_01_figure_03.png`
- [standard completion] `V(r)` or `E(r)` for the lower qualitative energy-versus-separation curve in `lecture_01_figure_04.png`
- [visible] `\text{Name}`
- [visible] `\text{Symbol}`
- [visible] `\text{type}`
- [visible] `\text{Photon}`
- [visible] `\gamma`
- [visible] `A`
- [standard completion] `A_\mu` as a cleaned modern completion of the photon field notation, only if the prose explicitly states that this goes beyond the literal board reading

## Diagram Extraction
- `lecture_01_figure_02.png`: Redraw a single localized bound-state profile near one proton; keep the screenshot visible because the lecture’s first sketch is part of the unfolding argument, not just a generic textbook figure.
- `lecture_01_figure_03.png`: Redraw the left-right pair of localized wave functions over two proton centers; keep the screenshot visible because the board layout itself shows the transition from one-center localization to a two-center problem.
- `lecture_01_figure_04.png`: Redraw both the upper two-center wave-function sketches and the lower qualitative potential-energy graph; keep the screenshot visible because the stacked board layout is pedagogically important.
- `lecture_01_figure_05.png`: Recast the visible photon row as a cleaned LaTeX table or aligned notation; keep the screenshot visible because it documents the actual board table and the placement of `\gamma` and `A`.
- No accepted frame directly captures the later worldline-style electron-exchange picture or the photon-exchange/Feynman-diagram picture, so those should be redrawn only from transcript evidence, not presented as board transcriptions.

## Reconstruction Guidance
- Keep all four screenshots in the final chapter as documentary evidence, but do not rely on enlarged raster images alone for the mathematical presentation.
- For `lecture_01_figure_02.png`, use a nearby TikZ sketch of a single smooth localized hump and describe it in prose as a schematic ground-state profile, not as a quantitatively labeled graph.
- For `lecture_01_figure_03.png`, use a nearby two-center TikZ redraw and, if helpful, place the approximate superposition \((\psi_1+\psi_2)/\sqrt2\) beside it, while explicitly noting that the exact equilibrium wave function is only approximately that sum.
- For `lecture_01_figure_04.png`, keep the lower graph qualitative. A clean redraw may introduce a conventional label such as \(V(r)\), but only if the prose makes clear that the image itself does not display a legible axis label.
- For `lecture_01_figure_05.png`, typeset only the secure part of the table: photon name, symbol `\gamma`, and field entry `A`. If the notes sharpen this to \(A_\mu\), mark that as cleaned standard notation rather than as a direct transcription.
- Prefer cautious completion from the transcript over invented detail: the images support the board layout and qualitative shapes, while the transcript supplies the intended physical meaning.

## Uncertainties
- The faint point labels in `lecture_01_figure_02.png` and `lecture_01_figure_03.png` look like `P`, but they are not reliably legible enough to assign a definite meaning.
- The curves in `lecture_01_figure_02.png` and `lecture_01_figure_03.png` are not explicitly labeled as `\psi`, so that identification comes from the transcript, not from visible chalk notation.
- In `lecture_01_figure_03.png`, the lecturer’s hand obscures part of the central overlap region, so the exact shape there should not be overinterpreted.
- In `lecture_01_figure_04.png`, the lower graph has no readable axis labels, so one should not pretend the frame alone distinguishes \(E(r)\) from \(V(r)\).
- In `lecture_01_figure_05.png`, the column above `A` is not fully visible, so the table structure beyond the secure visible entries is ambiguous.
- The handwritten `\gamma` in `lecture_01_figure_05.png` is legible, but only with transcript support; visually it could be mistaken for a Y-shaped symbol at first glance.