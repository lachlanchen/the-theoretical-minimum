# Visual Evidence
## Frame Inventory
- `lecture_02_figure_02.png`: A tight crop of the left board column showing the wave-relation stack centered on `\lambda f = c`; this screenshot should remain in the final notes as direct evidence for the photon-momentum substitution.
- `lecture_02_figure_03.png`: A wider board shot with Susskind pointing at the left column and a visible vertical divider between wave definitions and quantum/momentum formulas; this screenshot should remain in the final notes because the board structure itself is informative.
- `lecture_02_figure_04.png`: A right-board shot whose main payload is the upper-right photon-momentum formula `hf/c = h/\lambda`, with earlier energy and momentum formulas still partly visible to the left; this screenshot should remain in the final notes as direct evidence for the transition from the photon-specific formula to the more general wavelength form.
- `lecture_02_figure_05.png`: A clean shot of the nonrelativistic energy identity above a boxed frequency-wavelength relation for a slow particle; this screenshot should remain in the final notes as evidence for the structure and conclusion of the derivation.
- `lecture_02_figure_06.png`: A sparse sketch of a one-dimensional path labeled `L` with a marked end point, used to motivate periodic space without drawing a literal geometric circle; this screenshot should remain in the final notes and should be paired with a cleaned redraw.

## Equation Extraction
- `lecture_02_figure_02.png`: `\omega = 2\pi f` [visible]
- `lecture_02_figure_02.png`: `\lambda = \text{wave len...}` [partially visible]
- `lecture_02_figure_02.png`: `\lambda = \text{wavelength}` [standard completion]
- `lecture_02_figure_02.png`: `\lambda f = c` [visible]
- `lecture_02_figure_02.png`: `\dfrac{f}{c} = \dfrac{1}{\lambda}` [visible]

- `lecture_02_figure_03.png`: `f = \text{freq c/s}` [visible]
- `lecture_02_figure_03.png`: `f = \text{frequency}` [standard completion]
- `lecture_02_figure_03.png`: `\omega = 2\pi f` [visible]
- `lecture_02_figure_03.png`: `\lambda = \text{wave len...}` [partially visible]
- `lecture_02_figure_03.png`: `\lambda = \text{wavelength}` [standard completion]
- `lecture_02_figure_03.png`: `\lambda f = c` [visible]
- `lecture_02_figure_03.png`: `\dfrac{f}{c} = \dfrac{1}{\lambda}` [visible]
- `lecture_02_figure_03.png`: `\hbar = \dfrac{h}{2\pi}` [visible]
- `lecture_02_figure_03.png`: `E = \hbar\omega = \cdots` [partially visible]
- `lecture_02_figure_03.png`: `E = \hbar\omega = hf` [standard completion]
- `lecture_02_figure_03.png`: `\vec p = m\vec v` [visible]
- `lecture_02_figure_03.png`: `|p| = \dfrac{E}{c}` [visible]

- `lecture_02_figure_04.png`: `\hbar = \dfrac{h}{2\pi}` [partially visible]
- `lecture_02_figure_04.png`: `E = \hbar\omega = hf` [partially visible]
- `lecture_02_figure_04.png`: `\vec p = m\vec v` [partially visible]
- `lecture_02_figure_04.png`: `|p| = \dfrac{E}{c}` [partially visible]
- `lecture_02_figure_04.png`: `p_{\text{photon}} = \dfrac{hf}{c} = \dfrac{h}{\lambda}` [visible]

- `lecture_02_figure_05.png`: `\psi` [visible]
- `lecture_02_figure_05.png`: `E = \dfrac{1}{2}mv^2 = \dfrac{1}{2}\dfrac{p^2}{m}` [visible]
- `lecture_02_figure_05.png`: `f = \dfrac{1}{2m}\dfrac{h}{\lambda^2}` [visible]
- `lecture_02_figure_05.png`: `\dfrac{h}{\lambda}` [partially visible]

- `lecture_02_figure_06.png`: `L` [visible]

## Diagram Extraction
- `lecture_02_figure_02.png`: This is not a standalone diagram but a left-column derivation stack; preserve it as a screenshot, and typeset the cleaned equations nearby rather than redrawing it in TikZ.
- `lecture_02_figure_03.png`: The important visual content is the board organization itself, split into a left review column and a right application column by a vertical line; preserve it as a screenshot, but do not redraw the whole board in TikZ.
- `lecture_02_figure_04.png`: The visually meaningful structure is the boxed intermediate `hf/c` and the equality to `h/\lambda`; preserve the screenshot and reproduce the cleaned formula as display math rather than as TikZ.
- `lecture_02_figure_05.png`: The board has a clear derivational layout, with the energy identity above and the boxed frequency relation below; preserve the screenshot and reproduce the formulas as display math rather than as TikZ.
- `lecture_02_figure_06.png`: This should be shown both ways: preserve the screenshot as evidence of how Susskind first sketches the idea, and redraw it in TikZ as a cleaner identified interval or schematic loop of total length `L`.

## Reconstruction Guidance
- Use `lecture_02_figure_02.png` and `lecture_02_figure_03.png` to support a clean note-quality presentation of the wave preliminaries: define `f`, `\omega`, and `\lambda`, then write `\lambda f = c` and `f = c/\lambda` cleanly in typeset form.
- Use `lecture_02_figure_03.png` to preserve the lecture’s board logic: the left side contains definitions and wave kinematics, while the right side begins the quantum-mechanical and momentum discussion; the notes should mirror that progression even if they do not literally imitate the blackboard layout.
- Use `lecture_02_figure_04.png` to typeset the photon-momentum step as `p = hf/c = h/\lambda`, keeping the screenshot nearby because the boxed intermediate `hf/c` shows the derivation is being assembled from earlier relations rather than asserted from nowhere.
- Use `lecture_02_figure_05.png` to reconstruct the slow-particle derivation in clean algebraic order: `E = p^2/(2m)`, `p = h/\lambda`, and therefore `f = h/(2m\lambda^2)`; the screenshot should remain nearby because it documents the lecture’s emphasis on the final boxed relation.
- Use `lecture_02_figure_06.png` as visual evidence that the periodic-space discussion begins from a labeled path, not from a polished geometric circle; the TikZ redraw should make the endpoint identification explicit, but the prose should note that the geometry is schematic.
- Where the board writing is partial, prefer modest standard completion over full reconstruction of missing steps. In particular, complete truncated labels like “wave len...” to “wavelength,” but do not invent additional formulas that are not supported by either the frame or the transcript.

## Uncertainties
- In `lecture_02_figure_02.png` and `lecture_02_figure_03.png`, the word after `\lambda =` is only partially legible, so `\lambda = \text{wavelength}` is a cautious completion rather than a fully visible transcription.
- In `lecture_02_figure_03.png`, the shorthand `f = \text{freq c/s}` is compressed handwriting; the intended meaning is clear, but the exact chalk wording is abbreviated.
- In `lecture_02_figure_03.png`, the energy relation is cropped after `E=\hbar\omega=`, so the completion to `hf` is not fully visible in that frame alone.
- In `lecture_02_figure_04.png`, the handwritten label next to the momentum formula looks like a photon label, but the exact index or wording is not perfectly reliable; `p_{\text{photon}}` is the safest cleaned form.
- In `lecture_02_figure_04.png`, a boxed or bracketed note on the left seems to contain something like `Non Rel`, but it is not legible enough to quote confidently.
- In `lecture_02_figure_05.png`, the partial `h/\lambda` at the far left is only a fragment of an earlier line, so it should not be treated as standalone evidence for the full derivation.
- In `lecture_02_figure_06.png`, the screenshot does not itself show the endpoint identification that makes the space periodic; that interpretation comes from the surrounding lecture and should therefore be presented as a cautious reconstruction in any redraw.