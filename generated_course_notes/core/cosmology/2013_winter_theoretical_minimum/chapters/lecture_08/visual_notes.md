# Visual Evidence
## Frame Inventory
- `lecture_08_figure_02.png`: The frame shows the start of a proton-decay board sketch with a top horizontal line labeled `P` and a leftover baryon-number expression on the far left; this screenshot should remain in the final notes.
- `lecture_08_figure_03.png`: The frame shows a clearly written baryon-number formula above a new line beginning `C = ...` for charge conjugation; this screenshot should remain in the final notes.
- `lecture_08_figure_04.png`: The frame shows a partially written Schrödinger-equation cue on the upper left and a large circular multi-path arrow sketch on the right; this screenshot should remain in the final notes.
- `lecture_08_figure_06.png`: The frame shows the cleanest visible force-potential relation, with `V(\phi)` above `F=-dV/d\phi`, plus a simple left-side falling-object sketch; this screenshot should remain in the final notes.

## Equation Extraction
- `lecture_08_figure_02.png`: `P` [visible]
- `lecture_08_figure_02.png`: `\dfrac{N_q - N_{\bar q}}{3}` [partially visible]
- `lecture_08_figure_02.png`: `p \to e^+ + \gamma` [standard completion]

- `lecture_08_figure_03.png`: `B = \dfrac{N_q - N_{\bar q}}{3}` [visible]
- `lecture_08_figure_03.png`: `C = \text{Ch}\cdots` [partially visible]
- `lecture_08_figure_03.png`: `C = \text{charge conjugation symmetry}` [standard completion]

- `lecture_08_figure_04.png`: `i\,\dfrac{\partial \psi}{\partial t}` [partially visible]
- `lecture_08_figure_04.png`: `i\,\dfrac{\partial \psi}{\partial t} = \cdots` [standard completion]

- `lecture_08_figure_06.png`: `V(\phi)` [visible]
- `lecture_08_figure_06.png`: `F = -\,\dfrac{dV}{d\phi}` [visible]

## Diagram Extraction
- `lecture_08_figure_02.png`: The board contains only the opening segment of the proton-decay Feynman-style picture, so it should be shown both ways: preserve the screenshot as evidence and add a TikZ redraw of the completed process `p \to e^+ + \gamma` from the transcript.
- `lecture_08_figure_03.png`: This is primarily an equation-and-notation frame rather than a figure in the narrow sense; preserve it as a screenshot, and do not replace it with TikZ.
- `lecture_08_figure_04.png`: The right-hand circular sketch with several directed paths and two side junctions may be pedagogically useful, but it is too ambiguous to serve as a definitive standalone reconstruction; preserve the screenshot, and if needed add only a schematic TikZ redraw clearly marked as contextual rather than exact.
- `lecture_08_figure_06.png`: The left-side circled point with a downward arrow is a useful intuitive sketch for the falling-stone analogy; the screenshot should remain visible, and a small clean pedagogical redraw of the potential-and-force setup may be added nearby if the notes benefit from it.

## Reconstruction Guidance
- Use `lecture_08_figure_02.png` as visual evidence for how Susskind begins the decay diagram on the board, but reconstruct the actual physical process from the transcript rather than from the incomplete frame. The clean note version should show the incoming proton and outgoing \(e^+\) and \(\gamma\), while the screenshot stays nearby to preserve classroom sequencing.
- Use `lecture_08_figure_03.png` to anchor the transition from baryon number to discrete symmetries. In the notes, typeset the baryon-number formula cleanly and introduce \(C\) in polished prose or display math, while keeping the screenshot as evidence of board order and notation.
- Use `lecture_08_figure_04.png` only for a cautious Schrödinger-equation cue in the time-reversal discussion. The cleaned note version should present a standard schematic form such as \(i\,\partial_t\psi=\cdots\), without pretending that the occluded right-hand side is fully visible in the image.
- Treat the loop-like drawing in `lecture_08_figure_04.png` as board context rather than as a secure mathematical object. If it appears in the final notes, it should be as an approximate auxiliary sketch with the screenshot still present, not as a precise reconstruction.
- Use `lecture_08_figure_06.png` as the main board evidence for the force-potential relation. The clean note version should typeset \(V(\phi)\) and \(F=-dV/d\phi\) prominently, and then connect that relation to the later friction discussion and the scalar-field analogy.
- Where standard physics notation is used to clean up the board, keep the reconstruction modest: complete only what the transcript clearly motivates, and avoid adding extra derivation steps that are not present in the lecture.

## Uncertainties
- In `lecture_08_figure_02.png`, the leftover baryon-number expression is only partly visible, and the handwritten subscript is more reliably interpreted with help from `lecture_08_figure_03.png`.
- In `lecture_08_figure_02.png`, the actual decay branches are not yet drawn, so the process \(p \to e^+ + \gamma\) is transcript-supported rather than frame-complete.
- In `lecture_08_figure_03.png`, the text after `C =` is mid-word and not fully legible on the board.
- In `lecture_08_figure_04.png`, the equation is substantially occluded by Susskind’s head and hand, so only the left portion is reliable as visual evidence.
- In `lecture_08_figure_04.png`, the meaning of the circular multi-path arrow sketch is not securely recoverable from the frame alone.
- In `lecture_08_figure_06.png`, the handwritten field symbol could be read as either `\phi` or `\varphi`, though the transcript supports `\phi`.
- In `lecture_08_figure_06.png`, the left-side falling-object sketch is unlabeled, so it should be treated as intuitive context, not as a source of exact notation.