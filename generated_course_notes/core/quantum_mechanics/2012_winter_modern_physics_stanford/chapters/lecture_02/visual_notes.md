# Visual Evidence
## Frame Inventory
- `lecture_02_figure_02.png` shows a left-hand linear-combination expression and, on the right, two non-collinear vectors from a common origin; this screenshot should remain in the final notes as visual evidence for the spanning argument.
- `lecture_02_figure_03.png` shows the mapping from function notation to ket notation, with a second analogous line started below; this screenshot should remain in the final notes because the notation shift is both legible and conceptually important.
- `lecture_02_figure_06.png` shows a simple ordinary-vector sketch with a vertical reference line and two rightward rays; this screenshot should remain in the final notes as evidence for the blackboard-vector picture used to introduce operators.

## Equation Extraction
- `lecture_02_figure_02.png`: `r_1 V_1 + r_2 V_2` `[partially visible]`
- `lecture_02_figure_02.png`: `V_1` `[visible]`
- `lecture_02_figure_02.png`: `V_2` `[visible]`
- `lecture_02_figure_03.png`: `\psi(x) \Rightarrow |\psi\rangle` `[visible]`
- `lecture_02_figure_03.png`: `\phi(x)` `[visible]`
- `lecture_02_figure_03.png`: `\phi(x) \Rightarrow` `[partially visible]`
- `lecture_02_figure_03.png`: `\phi(x) \Rightarrow |\phi\rangle` `[standard completion]`
- `lecture_02_figure_06.png`: no stable symbolic equation is visible; the content is diagrammatic rather than algebraic.

## Diagram Extraction
- `lecture_02_figure_02.png`: redraw the right-hand vector sketch in TikZ and keep the screenshot visible nearby. The redraw should show two non-collinear vectors from a common origin; an interior third vector may be added only as a transcript-guided reconstruction, not as something visible in the frame.
- `lecture_02_figure_03.png`: preserve the screenshot and pair it with clean displayed equations rather than a TikZ figure. The board layout matters here mainly as evidence for the notation transition from functions to abstract vectors.
- `lecture_02_figure_06.png`: redraw the geometric sketch in TikZ and keep the screenshot visible nearby. The redraw should preserve the vertical guide/reference line and the two rightward slanted rays, while staying unlabeled unless the surrounding prose explicitly supplies labels from the transcript.

## Reconstruction Guidance
- For `lecture_02_figure_02.png`, the note-quality equation should be typeset cleanly as the linear combination of two spanning vectors, while the screenshot documents the actual board state and spatial layout. If the chapter explains that a third vector in the wedge is redundant, show that with a clean redraw and mark it as a reconstruction from the lecture discussion rather than from the frame itself.
- For `lecture_02_figure_03.png`, the top line is strong enough to become a displayed equation exactly in the notes. The lower line should be completed only cautiously: the image supports that Susskind is beginning the parallel example, while the transcript justifies the clean completion to `|\phi\rangle`.
- For `lecture_02_figure_06.png`, treat the screenshot as evidence of blackboard geometry and teaching layout, not as a source of extra notation. The TikZ version should simply make the geometry clearer for readers, with no added symbolic structure unless the transcript explicitly warrants it.
- Across all three frames, keep the screenshots when the board layout or historical chalk state matters, and use clean LaTeX or TikZ beside them to remove ambiguity. The notes should separate “this is what the board visibly shows” from “this is the cleaned mathematical statement.”

## Uncertainties
- In `lecture_02_figure_02.png`, the chalk lettering reads more like uppercase `V_1,V_2`, while the transcript speaks of `v_1,v_2,v_3`; the visible board does not settle the normalization of that notation.
- In `lecture_02_figure_02.png`, the first coefficient and part of the expression are slightly compromised by Susskind’s body position, so the full left-hand formula is only partially secure visually.
- In `lecture_02_figure_02.png`, no third vector is actually drawn inside the wedge in the selected frame; that idea comes from the spoken explanation.
- In `lecture_02_figure_03.png`, the arrow is a drawn implication/identification mark rather than a formally precise symbol, so the final notes should avoid overreading it as strict equality.
- In `lecture_02_figure_03.png`, the lower line is incomplete in the image, so `\phi(x) \Rightarrow |\phi\rangle` is a cautious completion, not a direct transcription.
- In `lecture_02_figure_06.png`, the vertical line may be an axis, a guide, or just part of a quick sketch; the image alone does not fix its intended interpretation.
- In `lecture_02_figure_06.png`, the two rays are clearly ordinary vectors, but the exact operation they are meant to support belongs to the nearby lecture context, not to the frame by itself.